# 跟車停等距離：完整研究與方案

**問題**：跟車停等時停得太近，駕駛要的是 5 m。
盤點日期 2026-09-14，對應 HEAD `19cbfc1cc`。控制鏈路的全貌見 `LONGITUDINAL.md`。

這份是「為什麼停太近、試過什麼、最後要做什麼」的單一入口。
**動手前先讀完第 3 節（被推翻的假設）**，裡面有七個今天一天內被自己的數據否決的推論。

---

## 1. 事實基線

### 1.1 停等距離的真實分布

| 來源 | n | 中位 | 10th–90th | 最近 |
|---|---|---|---|---|
| OP 控制（8 route、151 分鐘） | 20 | **3.57 m** | 2.31–4.64 | 2.02 |
| **駕駛自己踩煞車停**（同批資料） | 23 | **3.53 m** | 2.55–6.30 | 1.33 |
| OP（9/14 兩趟另外統計） | 15 | 3.64 m | — | 1.13 |

⚠ **OP 和駕駛自己停的距離幾乎一樣**，OP 的分布還更集中。
「OP 比人停得近」這個前提在數據上不成立 —— 差別在**節奏**不在**結果**。

⚠ 影片核對 4 次「停太近」事件（`E:/Temp/video0914/`）：其中一次（dRel 2.83 m）
雷達鎖的是**騎在正前方的機車**，不是後面那台白色轎車。跟機車 2–3 m 是正常的。
**用 `dRel < 3.5 m` 篩「停太近」會混進機車事件**，統計時要留意。

### 1.2 接近過程

停車前各時段，「從此刻起用固定減速度、剛好停在 5 m」所需的值 vs 實際做的：

| 停前 | 需要 | OP 實際 | 差 | 駕駛實際 | 差 |
|---|---|---|---|---|---|
| 10–12 s | −0.57 | −0.31 | +0.15 | −0.34 | −0.02 |
| 8–10 s | −1.03 | −0.63 | +0.16 | −0.47 | +0.09 |
| 6–8 s | −0.80 | −0.80 | −0.04 | −0.80 | +0.06 |
| 4–6 s | −1.11 | −0.85 | +0.23 | −0.78 | +0.08 |
| 3–4 s | −1.24 | −1.08 | +0.30 | −0.88 | +0.15 |
| 2–3 s | −0.90 | −0.87 | +0.15 | −1.22 | −0.11 |
| 1–2 s | — | — | — | −1.06 | **−0.40** |
| 0–1 s | — | — | — | −1.04 | **−0.75** |

**差別在最後兩秒**：駕駛會補一腳（−0.40 / −0.75 比需求更重），OP 不會。

### 1.3 最溫和的可行解（9/14 那 15 次，逐次）

| 最後停在 | 整個接近過程中最溫和只需要 | 那一刻 | OP 實際最重 |
|---|---|---|---|
| 1.52 m | **−0.27 m/s²** | 16.9 km/h、33.1 m 外 | −0.95 |
| 2.14 m | −0.32 | 44.7 km/h、28.1 m | −2.62 |
| 2.83 m | −0.39 | 13.0 km/h、21.6 m | −1.46 |
| 3.96 m | −0.12 | 27.9 km/h、32.3 m | −2.31 |

**「提早煞就好」是對的** —— 20–33 m 外只需要 −0.1 ~ −0.4（幾乎感覺不到）
就能穩穩停在 5 m。OP 卻拖到最後用 3–8 倍重的煞車，還是停得更近。

---

## 2. 機制：為什麼它不提早煞

### 2.1 在遠處它不認為有問題

MPC 的目標間距是 `v²/2CB + t_follow·v + STOP_DISTANCE`。以停 1.52 m 那次為例：

| | |
|---|---|
| 當下 | 16.9 km/h（4.7 m/s）、前車**靜止**在 33.1 m 外 |
| MPC 目標間距 | 4.7²/3 + 1.5×4.7 + 7 = **21.4 m** |
| 它看到的誤差 | **+11.7 m（還太遠，可以繼續接近）** |

**它不知道前面那台是停的**，只知道「現在比目標遠」。等 gap 掉進 21.4 m 才出現負誤差，
那時速度還沒降，溫和的解已經過期。

### 2.2 距離在成本裡太便宜

`long_mpc.py:227-233`：

```python
costs = [((x_obstacle - x_ego) - desired_dist) / (v_ego + 10.),   # w = 3
         ..., a_ego - a_prev,                                      # w = 200
         j_ego]                                                    # w = 5
```

停車時（v≈0，除以 10）：車距少 1 m 罰 `3·(1/10)² = 0.03`；
加速度比上一幀多變 0.1 罰 `200·0.1² = 2.0`。

→ **MPC 願意用 8.2 m 車距換「加速度不要多變 0.1 m/s²」**。

理論上停住時 `gap` 應該收斂到 `desired_dist(v=0) = STOP_DISTANCE = 7 m`，
實際停在 3.6 m —— **穩態誤差 3.4 m**，而那 3.4 m 只值 0.35 分，抵不過任何加速度調整。

危險區約束（`:241-245`，`DANGER_ZONE_COST=100`，底線 `0.75 × 目標`）同樣被 `/(v+10)` 除過：
停在 4 m 只罰 1.56 分 ＝ 只值 0.088 的加速度變化。**連安全底線都是可買賣的。**

---

## 3. 被推翻的假設（2026-09-14 一天內，全部由數據否決）

| # | 曾經以為 | 實測結果 |
|---|---|---|
| 1 | `STOP_DISTANCE` 是旋鈕 | 6.0→7.0 停等距離幾乎沒動。⚠ **但這是跨不同趟比較，不是控制實驗**（9/8 23:15 改的），證據強度比想像中弱 |
| 2 | `COMFORT_BRAKE` 降低會讓它提早煞 | 2.0→1.5 停等距離**完全沒動**，卻讓 `min()` 換贏家 +52%、跟車 jerk 惡化。已回退 `19cbfc1cc` |
| 3 | 降 `COMFORT_BRAKE` 會讓車「變猛」 | **反向**：前車拉開時的指令從 +0.655 降到 +0.328。算式對，但被 `min()` 的 cruise 天花板蓋住 |
| 4 | `longcontrol` 的 `stopping` 斜坡忽略 `a_target` 是主因 | 否決：只在 `v < 0.3 m/s`（1.1 km/h）才接管，那時車距已定，停住後只再前進 0.05 m |
| 5 | 執行端不照指令煞（小指令吃掉了） | 否決：`longcontrol 送出/planner 要的 = 1.00`，`車身做到/要的 = 0.79–1.23`。**執行鏈路是乾淨的** |
| 6 | 雷達在接近過程掉失導致晚煞 | 否決：15 次裡 11 次全程 100% 看得到，中位 100%，掉失平均 0.3 次。與停住距離相關係數僅 +0.26 |
| 7 | 要新寫一條幾何式停止律 | `junction_handoff.py:190-195` 已經有一份（`−v²/2d`、clamp −2.5、rate limit 4.0 m/s³）。差點寫第二份 |

---

## 4. 其他 fork 怎麼做（7 個 fork 全查過）

| 做法 | 誰 | 評估 |
|---|---|---|
| `increased_stopping_distance`：`x_lead -= D` 餵給 MPC，且隨速度淡出 | FrogPilot `long_mpc.py:327-330,359`，UI 滑桿 0–5 m | **數學上等價於加大 `STOP_DISTANCE`**（都是讓誤差減少 D），但多了「只在低速生效」→ 不動跟車距離。這是它唯一的優勢 |
| `dynamic_t_follow`：用實際 `dRel` 與目標的差回授修正 `t_follow` | carolpilot `carrot_functions.py:212-227` | P 控制，能縮小但不能消除穩態誤差 |
| `fcw_stop`：`dRel < 4 m` 時強制進 `stopping` | carolpilot `longcontrol.py:41-46` | 安全網，不治本 |
| 速度分段 cost weights | StarPilot | 低速段刻意維持 stock 的 3/200，對本問題無幫助 |

⚠ **7 個 fork 沒有一個動過 `/(v_ego+10)` 正規化，也沒有一個對 MPC 輸出加 rate limit。**
我們診斷出的根因沒有現成答案可抄。

---

## 5. 閉環 MPC 重放（在車機上跑，acados 只有車機有）

`E:/Temp/drive0914e/replay_device.py` + `cases.npz`，車機 `/data/replay/`。
1.16 ms/solve，7 個停車事件可重放。前車用**絕對位置**還原，我們自己的位置由模擬積分
（開環估算不算數）。

⚠ **保真度不足，只能看相對趨勢**：base 方案預測中位 4.90 m，實際 3.64 m（偏遠 1.3 m），
兩個 case 因為雷達記錄本身在跳（gap 32→68→0→42）而發散。
掃過 `tau_act` 0.15–0.7 都無法收斂到實際值。差異來源尚未找到 —— 執行端與雷達品質都已排除。

相對趨勢（同一模擬誤差下比較）：

| 方案 | 停住距離變化 |
|---|---|
| `lead_offset +2m`（FrogPilot 式） | **+1.96 m**（傳遞率 ~98%） |
| `lead_offset +4m` | +3.93 m |
| `X_EGO_OBSTACLE_COST` 3→10 | +0.11 m |
| `X_EGO_OBSTACLE_COST` 3→30 | +0.72 m |
| `A_CHANGE_COST` 200→100 / →50 | −0.01 / −0.09 m（**無效**） |
| 幾何 `a_floor` 目標 5 m | +0.17 m |
| 幾何 `a_floor` 目標 6 m | +1.02 m |

⚠ **`a_floor` 在模擬中效果被低估**：模擬的 MPC 表現比實車好（停 4.9 vs 3.6），
floor 沒機會咬。實車 MPC 更寬鬆，開環估算顯示它會咬 16/29 次。

⚠ **模擬的 `lead_offset` 傳遞率 98% 與實車 `STOP_DISTANCE` 的 0.1–0.3 直接矛盾。**
兩者數學上是同一個槓桿。這個矛盾**尚未解決**，是目前最大的不確定性。

---

## 6. 方案

### 6.1 為什麼選幾何律而不是調參數

調參數（`STOP_DISTANCE` / cost weights）都是在跟成本函數談判，而距離在那本帳上太便宜。
幾何律 `a_floor = −(v² − v_lead²) / (2(dRel − D))` 的力道是**剩餘距離的函數**，
距離越小力道越大，**數學上保證停在 D**，不參與競價。

同一件事在紅綠燈停止上已經驗證過：用定速律（速度的函數）**36/36 全部衝過停止線**，
中位超出 8.6 m；換成幾何律才停得住。

### 6.2 設計（最小改動）

只動 `junction_handoff.py`，加一條**獨立分支**，不碰既有的 `lead_relevant` 邏輯
（那段的設計初衷就是「前車幾乎不動＝車隊，讓給 MPC」，反轉它會重新引入它原本要防的失效）。

```
新分支（與既有的模型計畫分支平行，各算各的候選，取更保守者寫進同一個 a_floor）：
  武裝：雷達確認的前車 + vLead 低於門檻（雙門檻滯後，避免 0.5 m/s 附近抖動）
       + dRel < 40 m + v_ego > 1 m/s
  want = −min((v² − v_lead²) / (2(dRel − D_TARGET)), MAX_FLOOR_DECEL=2.5)
  rate limit：沿用 FLOOR_JERK = 4.0 m/s³
  D_TARGET = 5.0（雷達 dRel 比實際近 0.85 m，所以眼睛看到的會是約 5.8 m）
```

四個必要護欄（第一版量化時全部踩過）：
1. **clamp −2.5**：不 clamp 時公式會爆到 −10.9 / −15.5 m/s²
2. **`dRel < 40 m` 才武裝**：不設門檻會在 57 m 外、MPC 還想加速時就壓成 −0.93
3. **`vLead` 平滑 + 雙門檻**：用瞬時值會有 13.7% 的介入是誤判（前車其實在動）
4. **不能放在 `_turning()` 的 `reset()` 之後**：打方向燈時整個物件被 reset，
   前車不會因為打燈就消失

### 6.3 開環量化（`E:/Temp/drive0914/analyze15.py`，D=5.0）

| | |
|---|---|
| 29 次停車，會介入 | **16 次** |
| 這 16 次現在停在 | 中位 **3.99 m**（就是太近的那批） |
| 沒介入的 13 次現在停在 | 中位 **5.95 m**、**沒有一次 < 3 m** |
| 全趟壓住 MPC 的幀 | **0.69%** |
| 前車其實在動的誤判 | **0 幀** |
| 力道 | 中位 −1.89，最重 −2.5（clamp），多數只比 MPC 多要 −0.08 ~ −0.2 |

**選擇性正確**：只咬停太近的那批，本來停得好的一次都沒碰。

### 6.4 不做的事

- **不調 `STOP_DISTANCE`**（駕駛指定為紅線；而且它是全速域常數，會等量加大跟車距離）
- **不調 `A_CHANGE_COST`**（重放顯示無效，且會讓所有速域變抖）
- **不改 `/(v_ego+10)`**（7 個 fork 沒人動過，風險未知）
- **不改 `lead_relevant`**（會反轉 junction 原本的設計意圖）

---

## 7. 驗證計畫

1. **上車前**：對 15 次停等重放，確認「最溫和可行解」被更早採用、`|a|` 不超過 2.5
2. **上車前**：對一般跟車路段重放，確認新分支沒有在非進場階段介入
3. **上車前**：打方向燈 + 前方有停止車的併發情境，確認 `_turning()` 沒把 floor 清掉
4. **上車後**：同一條路來回，**同一趟內**比較（不要再跨趟比較 —— 第 3 節 #1 就是這樣錯的）
5. **上車後**：確認 HUD 顯示什麼（目前 `stopLight` 已經分不出三種 e2e 來源，加第四種會更糟，
   建議在 `custom.capnp` 加 `leadStop` enum —— capnp 只能新增不能改既有編號）

---

## 8. 尚未解決

- **模擬 `lead_offset` 傳遞率 98% vs 實車 `STOP_DISTANCE` 0.1–0.3 的矛盾**（第 5 節）
- **閉環重放偏遠 1.3 m 的來源**（執行端、雷達品質都已排除）
- **OP 和駕駛自己停一樣近**（3.57 vs 3.53）—— 需要確認駕駛要的 5 m 是「比現在遠」還是
  「比現在穩定」，這兩個要的方案不同

---

## 9. 全線掃描（2026-09-15，60614 幀、11 route、涵蓋到 112 km/h）

前面幾節只看停車，不足以判斷「改 MPC 權重」的影響 —— 權重是全域的。
這節把所有情境都量了，並且**套上 planner 的 `min(MPC, a_cruise)`**（只看 MPC 原始輸出會
重演 9/14 早上「算式對但預測錯」的那個錯）。

### 9.1 各情境的變化（套天花板後，指令中位，括號為 90th）

| 情境 | n | obs30 | obs30+a_change300 | obs100 |
|---|---|---|---|---|
| 穩態跟車 \|vRel\|<1 | 15669 | +0.060 (+0.03) | +0.067 (+0.03) | +0.103 (+0.06) |
| 接近 vRel<-1 | 6185 | **−0.282** (+0.01) | −0.136 (+0.02) | −0.619 (+0.00) |
| 拉開 vRel>+1 | 7081 | +0.058 (+0.18) | +0.040 (+0.14) | +0.077 (+0.26) |
| 停車接近 | 2404 | **−0.099** (+0.11) | +0.009 (+0.10) | −0.303 (+0.17) |
| 市區 0-40 有前車 | 14534 | +0.102 (+0.15) | +0.093 (+0.12) | +0.168 (+0.23) |
| 郊區 40-72 有前車 | 6462 | +0.145 (+0.01) | +0.155 (+0.01) | +0.175 (+0.01) |
| 高速 72+ 有前車 | 7939 | +0.089 (+0.01) | +0.100 (+0.01) | +0.150 (+0.02) |

⭐ **`min()` 的天花板吸收了大部分副作用**：拉開情境下 obs30 的 MPC 想要 +1.103，
實際只送出 +0.481，**68.6% 的幀被天花板擋住**（base 是 40.4%）。

### 9.2 停車達成率（對「停在 5 m」，套天花板後）

| | 達成率 | 指令中位 | 最重 10th |
|---|---|---|---|
| base (3/200) | 83% | −0.648 | −1.695 |
| obs10 | 87% | −0.660 | −1.754 |
| **obs30** | **99%** | −0.747 | −1.898 |
| obs30 + a_change300 | 87% | −0.639 | −1.767 |
| obs100 | 126% | −0.952 | −2.269 |
| 實車記錄 | 95% | −0.659 | — |

### 9.3 jerk（20 Hz、a_prev 用自己上一幀的輸出）

| | 中位 | 90th | 99th | >2 佔比 |
|---|---|---|---|---|
| base (3/200) | 0.126 | 0.81 | 2.92 | **2.13%** |
| **obs30** | 0.152 | 1.14 | 4.29 | **4.05%** |
| obs30 + a_change300 | 0.143 | 1.01 | 3.48 | 3.13% |
| 實車記錄 | 0.120 | 1.10 | 4.13 | 3.26% |

⚠ **停車準度與 jerk 是對沖的**：`a_change 300` 壓得住 jerk，但停車達成率掉回 87%。
兩者不能兼得。

### 9.4 `lead_stop` 的接手跳變（未解決）

用 20 Hz + 正確的 planner 迴圈重測，`lead_stop` 的結果比 5 Hz 版差很多：

| | 5 Hz（錯的） | 20 Hz（對的） |
|---|---|---|
| 壓住幀數 | 1.61% | **7.66%** |
| jerk >2 | +0.04 pp | **+1.17 pp** |
| 接手/放手 | 0.95 次/分 | **10.55 次/分** |
| 每段持續 | 中位 1.38 s | **中位 0.05 s，125/173 段短於 0.3 s** |

⛔ **根因在 planner 的咬住條件 `floor < min(output, 0.0)`**：當 MPC 要的是 +0.73 時，
這個條件等於 `floor < 0`，所以 floor 必須先降到負值才會生效，而那一刻它跟 output
差了 0.8 —— 一步跳 19.98 m/s³。試過讓 floor 從 `a_prev` 起 ramp，**沒有解決**，
因為卡點是那個 `min(…, 0.0)`，不是 ramp 的起點。junction 的 `a_floor` 有同樣的結構，
只是它觸發率只有 2.21% 所以沒被看見。

要修需要把 floor 的語意從「負的下限」改成「輸出的上限」（未作用時回傳 `ACCEL_MAX`），
planner 端改成 `min(output, floor)`。**尚未做。**

### 9.5 結論

| | 修根因 `X_EGO_OBSTACLE_COST 3→30` | 外加 `lead_stop` |
|---|---|---|
| 改動 | **一個數字** | 新模組 + 3 檔 |
| 停車達成率 | **83% → 99%** | 幾何保證（未驗證） |
| jerk >2 | 2.13% → 4.05%（+1.9 pp） | +1.17 pp，**但有 0.05 s 的 chatter** |
| 新接縫 | **沒有**（輸出本來就連續） | **有，且未解決** |
| 影響範圍 | 全部跟車 | 7.66% 的幀 |

**選 `X_EGO_OBSTACLE_COST = 30`。** 它修的是根因、不引入新接縫、不加機制。
代價是全域 jerk 從 2.13% 升到 4.05%（實車現況是 3.26%，模擬的 base 偏平順，
所以實際惡化可能小於 1.9 pp）。

`lead_stop.py` 留在 repo 但**不啟用**，直到 9.4 的接手跳變修好為止。
